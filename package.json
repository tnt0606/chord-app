{
  "name": "chord-app",
  "version": "0.1.0",
  "private": true,
  "description": "Chord chart web app for ukulele and guitar",
  "scripts": {
    "start": "live-server --port=8000",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
